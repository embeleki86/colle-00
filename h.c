#include <unistd.h>

int fn(int i);
  int ft_recursive_factorial(int nb);
{
 if (i <= 9)
   {
     i += 1;
     write(1 ,"d", 1);
  fn(i);
  write(1,"f",1);
  return 0;
   }
 else
 {
  return 0;
 }
}

int main()
{
  fn(0);
  return 0;
}
